# Database Configuration
DATABASE_URL="postgresql://dmk:dmk@localhost:5432/dmk"

# Authentication Secrets
NEXTAUTH_SECRET="your-super-secret-key-change-in-production-minimum-32-characters"
NEXTAUTH_URL="http://localhost:3001"

# Application Environment
NODE_ENV="development"
PORT=3001
HOST="127.0.0.1"

# Optional: Enable debug logging in development
DEBUG="friday-prayer:*"

# Production Note:
# In production, change the following:
# 1. Use a strong, unique NEXTAUTH_SECRET (generate with: openssl rand -hex 32)
# 2. Update DATABASE_URL with production credentials
# 3. Set NEXTAUTH_URL to your domain (https://yourdomain.com)
# 4. Set NODE_ENV to "production"